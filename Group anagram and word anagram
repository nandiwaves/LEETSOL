Anagrams are words made from exactly the same letters, just rearranged.

group ={}
for words in strs
  key = "".joined(sorted(word)
  if key in group
    group[key].append(word)
  else
    group[key] = word
//return all the groups of anagram as a list
  return list(group.values())

Anagrams are words made from exactly the same letters, just rearranged.
1. Create an empty dictionary.
2. Loop through each word.
3. Sort the word and join it to create the key.
4. If the key already exists, append the word to that list.
5. Otherwise, create a new list containing that word.
6. Return all the groups using:
   return list(group.values())
